import * as colors from './colors';

export const grandCentral = {
  background: `linear-gradient(180deg, ${colors.sun} 0%, ${colors.shade} 100%)`,
};

export const river = {
  background: `linear-gradient(180deg, ${colors.overcast} 0%, ${colors.river} 100%)`,
};
